
const initialState = {
    searchExpression: null,
    currentResult: [],
    currentUser: null
}

export default (state = initialState, {type, payload}) => {
    switch(type){
        default:
            return state;
    }
}